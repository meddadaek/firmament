"""Firmament simulation engine: the world, the firm's economy and the agents' bodies.

The agents' *decisions* live behind the Policy interface (see policy.py) so the
rule-based placeholder can be swapped for an LLM brain without touching the sim.
"""
